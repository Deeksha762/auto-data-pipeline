# Auto Data Pipeline

This project cleans and visualizes sales data using Python, Docker, and Jenkins.

## How to run
1. Build Docker image:
   docker build -t auto-data-pipeline .

2. Run container:
   docker run --rm auto-data-pipeline
